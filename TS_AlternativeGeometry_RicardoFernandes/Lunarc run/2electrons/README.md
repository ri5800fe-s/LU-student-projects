# 2 electron/event

For this case, there are three folders: `Pileup`, `Main`, and `Config`.

The `Pileup` and `Main` folders should be run first. After both have finished, you can run the files in the `Config` folder, which will merge the outputs from the two previous folders.
